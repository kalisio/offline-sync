{
  "name": "offline-sync",
  "version": "1.0.0",
  "description": "## Goal",
  "private": true,
  "main": "index.js",
  "scripts": {
    "compile": "lerna run compile",
    "test": "yarn compile && lerna run test",
    "dev": "./dev.sh"
  },
  "workspaces": [
    "packages/*",
    "example/*"
  ],
  "keywords": [],
  "author": "Kalisio",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^8.2.2"
  }
}
