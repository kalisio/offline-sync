{
  "name": "offline-sync",
  "version": "1.0.0",
  "description": "## Goal",
  "private": true,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "compile": "lerna run compile",
    "install": "yarn run compile",
    "test": "yarn compile && lerna run test",
    "dev": "./dev.sh",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "initialize": "node example/server/initialize.js"
  },
  "workspaces": [
    "packages/*",
    "example/*"
  ],
  "keywords": [],
  "author": "Kalisio",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
